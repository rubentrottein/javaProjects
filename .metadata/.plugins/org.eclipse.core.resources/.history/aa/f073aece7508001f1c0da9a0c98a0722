package test;

public class Start {

	public static void main(String[] args) {
		
		String separator = "--------------------";
		// TODO Auto-generated method stub
		System.out.println("Hello World");
		/*public static operate(int i) {
			return 5 + i;
		};
		System.out.print(operate(10));
		*/
		int iterator;
		iterator = 2;
		
		double d = 0.1;
		char c = 'c';
		boolean b = false;
		
		//String
		String s;
		
		System.out.println("	Ma variable iterator vaut  " + iterator + ". ");
		
		
		/* foreach *
		String [] daysOfWeek = new int [6] {"Lundi","Mardi","Mercredi", "Jeudi", "Vendredi", "Samedi","Dimanche"};
		
		for(String day: daysOfWeek){
			System.out.println(day);
		}
		
		/*while*/
		int j = 0;
		while(j<5) {
			System.out.println("ok :" + j);
			j++;
		}
		
		// do while
		int k = 0;
		do {
			System.out.println(" variabl k : " + k);
		}
		while(k!=0);

		// do while
		int l = 0;
		if(l==0) {
			System.out.println("if l " + l);
		} else if (l==1) {
			
		} else {
			System.out.println("else l " + 1);
		}
		/*
		public function testSwitchVariable() {
	
			switch(int variableToTest) {
				case 1:
					return "Cas n°1");
					break;
				case 2:
					return "Cas n°2");
					break;
				case 3:
					return "Cas n°3";
					break;
				default :
					return "Default";
				}
			}
		}
		*/
		int variableToTest = 1;
		//testSwitchVariable(variableToTest);
		int [] tabInt2 = new int[100];
		tabInt2[0] = 0;
		System.out.println(tabInt2);
		System.out.println(separator);
		
		int [] pairTab = new int[100];
		for(int i=0; i<pairTab.length; i++) {
			pairTab[i] = i;
			if(i%2==0) {
				System.out.print(pairTab[i] + " | ");
			}
			//i ? 50 : System.out.println(" firstHalf ");
		}
		
		System.out.println("\n" + separator);
		
		/**************************************** Copie des données d'un array vers un autre ****************************************/
		
		
		// Déclaration des arrays
		int [] originalTab = new int[10];
		int [] copyTab = new int[10];
		int [] invertTab = new int[10];

		public static void fill() {

			// Hydratation des arrays
			for(int i =0;i<originalTab.length;i++) {
				originalTab[i] = i;
			}
		}
		
		public static void copy(int [] original, int [] copy) {
			for(int i = 0;i<original.length;i++) {
				copy[i] = original[i];				
			}
		}
		
		public static void inversion(int [] original, int [] copy) {
			for(int i =0;i<original.length;i++) {
				invert[invert.length - i - 1] = copy[i];				
			}
		}

		public static void displayTab(String name, int [] tabToPrint) {
			// Affichage des arrays
			
			System.out.println(name);
			for(int valueToPrint : tabToPrint) {
				System.out.println(valueToPrint);			
			}
			System.out.println(separator);
		}
	}
}
